[package]
name = "risk_engine"
version = "0.1.0"
edition = "2021"

[dependencies]
core_types = { path = "../core_types" }
serde = { workspace = true, features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tempfile = "3.2"
