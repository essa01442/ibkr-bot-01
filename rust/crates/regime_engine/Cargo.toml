[package]
name = "regime_engine"
version = "0.1.0"
edition = "2021"

[dependencies]
core_types = { path = "../core_types" }
serde = { workspace = true, features = ["derive"] }
log = { workspace = true }
